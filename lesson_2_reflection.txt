What happens when you initialize a repository? Why do you need to do it?
A .git folder is created and all the files and subfolder of the directory start getting tracked.
How is the staging area different from the working directory and the repository? What value do you think it offers?
Staging area contains files which have updated since last commit and are ready for a new commit.